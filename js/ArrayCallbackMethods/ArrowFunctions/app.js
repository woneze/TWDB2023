// const add = function (x, y) {
//   return x + y;
// };

const add = (x, y) => {
  return x + y;
};

const square = (num) => {
  return num * num;
};

const rollDie = () => {
  return Math.floor(Math.random() & 6) + 1;
};
